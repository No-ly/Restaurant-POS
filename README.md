# Restaurant POS

A modern Point of Sale (POS) system designed for restaurants, built with a robust Node.js backend and a dynamic Angular frontend.

## 🚀 Tech Stack

- **Frontend:** Angular 19+, Bootstrap 5, FontAwesome
- **Backend:** Node.js, Express.js, MySQL
- **Database:** MySQL

## 🛠️ Installation & Setup

### Backend

1. Navigate to the backend directory:
   ```bash
   cd backend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Configure your environment variables in `.env` (ensure database credentials are correct).
4. Start the server:
   ```bash
   node src/index.js
   ```

### Frontend

1. Navigate to the frontend directory:
   ```bash
   cd frontend
   ```
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the development server:
   ```bash
   ng serve
   ```
4. Open your browser and visit `http://localhost:4200`.

---

# Restaurant POS (Español)

Un sistema de Punto de Venta (POS) moderno diseñado para restaurantes, construido con un backend robusto en Node.js y un frontend dinámico en Angular.

## 🚀 Tecnologías

- **Frontend:** Angular 19+, Bootstrap 5, FontAwesome
- **Backend:** Node.js, Express.js, MySQL
- **Base de Datos:** MySQL

## 🛠️ Instalación y Configuración

### Backend

1. Navega al directorio del backend:
   ```bash
   cd backend
   ```
2. Instala las dependencias:
   ```bash
   npm install
   ```
3. Configura tus variables de entorno en el archivo `.env` (asegúrate de que las credenciales de la base de datos sean correctas).
4. Inicia el servidor:
   ```bash
   node src/index.js
   ```

### Frontend

1. Navega al directorio del frontend:
   ```bash
   cd frontend
   ```
2. Instala las dependencias:
   ```bash
   npm install
   ```
3. Inicia el servidor de desarrollo:
   ```bash
   ng serve
   ```
4. Abre tu navegador y visita `http://localhost:4200`.
